<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Profile</title>

    <style>
        .center_profile {
        margin: 0;
        position: absolute;
        top: 50%;
        left: 50%;
        -ms-transform: translate(-50%, -50%);
        transform: translate(-50%, -50%);
        }
    </style>

</head>
<body>
    
    {{> header }}

    {{#if result}}

    {{else}}
        <div class="center_profile">
            <div class="card" style="width:400px">
                <img class="card-img-top" src="public/img/customer_img.png" alt="Card image" style="width:100%">
                <div class="card-body">
                    <h4 class="card-title">Hello {{user.username}}</h4>

                    {{#ifeq user.lastFen 'start'}}
                        <h3>No un-finished games found</h3>
                    {{/ifeq}}

                    {{#ifnoteq user.lastFen 'start'}}
                        <button onclick="location.href='/game/rejoin'" class="btn btn-primary">Re-join last game</button>
                    {{/ifnoteq}}
                </div>
            </div>
        </div>
    {{/if}}

</body>
</html>