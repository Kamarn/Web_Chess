<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.7/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <title>index</title>
</head>
<body>
    
    {{> header }}
    <style>
        .image-wrapper {
            position: relative;
            display: inline-block;
        }

        .alt-text {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            padding: 8px;
            background-color: rgba(0, 0, 0, 0.75);
            color: #fff;
            font-size: 14px;
            text-align: center;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            transition: all 0.3s;
        }

        .alt-text:hover {
            white-space: normal;
            overflow: visible;
        }
    </style>
    
    <script>
        $(function () {
            $('.image-wrapper').hover(
                function () {
                    var fullAlt = $(this).find('img').attr('data-full-alt');
                    $(this).find('.alt-text').text(fullAlt);
                },
                function () {
                    var truncatedAlt = $(this).find('img').attr('alt');
                    $(this).find('.alt-text').text(truncatedAlt);
                }
            );
        });
    </script>

    <div class="container">
        <div class="row">
            <div class="col-md-12 main">
                {{#each products}}
                    <div class="col-sm-6 col-md-3 product">
                        <div class="thumbnail">
                            <a href="/productinfo/{{productName}}">
                                <div class="image-wrapper">
                                    <img src="{{productImage}}" alt="{{productName}}" data-full-alt="{{productName}}" style="width:287px;height:400px;">
                                    <div class="alt-text">{{productName}}</div>
                                </div>
                            <div class="caption">
                                <p class="price" style="font-size:18px;">Price: {{productPrice}} $ </p>
                                <a href="/cart/add/{{productID}}" type="button" class="btn btn-primary btn-block view-product">Add to cart</a>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        </div>
    </div>

</body>
</html>