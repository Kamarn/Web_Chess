<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>index</title>

    <style>
        .center_index {
            margin: 0;
            position: absolute;
            top: 50%;
            left: 50%;
            -ms-transform: translate(-50%, -50%);
            transform: translate(-50%, -50%);
        }

        .btn-lg { 
            margin-top: 10px;
        }
    </style>

</head>
<body>
    
    {{> header }}
    
    {{#if user }}

        <div class="center_index">
            <div class="form-group">
                <input type="text" class="form-control" id="codeInput" aria-describedby="codeInput" placeholder="Code for match">
            </div>

            <button onclick="location.href='/game/create?code=' + $('#codeInput').val()" class="btn btn-primary btn-lg active">Create</button>
            <button onclick="location.href='/game/join?code=' + $('#codeInput').val()" class="btn btn-primary btn-lg active">Join</button>

            {{#if message}}
                <h4 class="alert alert-danger mt-4">{{message}}</h4>
            {{/if}}

        </div>          
        
                        
    {{else}}
        <center>
            <h1>Please log in to play</h1>
        </center>
    {{/if}}


</body>
</html>